# 独立域名部署

Vite 和 BrowserRouter 均使用站点根路径。边缘代理将 `/api/` 原样转发给后端，其余请求转发给前端容器；前端 Nginx 保留 SPA fallback，静态资源不存在时返回 404。

发布后检查登录页、JS/CSS 及 API 的响应类型。浏览器存储按 origin 隔离，更换域名后用户需要重新登录。服务器域名、凭据及其他运行环境配置不写入本仓库。
